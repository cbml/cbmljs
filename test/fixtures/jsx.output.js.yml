language: cbml
tag: '#cbml'
attributes: []
type: CBMLElement
body:
  - type: TextNode
    content: |2-
       {/*<Checkbox
          defaultChecked={typeof data.type !== 'undefined' ? data.type == 0 : isIPage}
          name="isFolder"
          label="是否为文件夹"
          disabled={showLoading || isIPage || !!data.id}
        />*/}
    loc:
      source: |2-
         {/*<Checkbox
            defaultChecked={typeof data.type !== 'undefined' ? data.type == 0 : isIPage}
            name="isFolder"
            label="是否为文件夹"
            disabled={showLoading || isIPage || !!data.id}
          />*/}
      start:
        line: 1
        column: 1
      end:
        line: 6
        column: 8
loc:
  source: |2-
     {/*<Checkbox
        defaultChecked={typeof data.type !== 'undefined' ? data.type == 0 : isIPage}
        name="isFolder"
        label="是否为文件夹"
        disabled={showLoading || isIPage || !!data.id}
      />*/}
  start:
    line: 1
    column: 1
  end:
    line: 6
    column: 8
